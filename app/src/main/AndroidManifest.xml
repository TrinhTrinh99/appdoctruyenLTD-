<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdoctruyen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appdoctruyen">
        <activity android:name=".MainDangBai" />
        <activity android:name=".MainAdmin" />
        <activity android:name=".MainThongTin" />
        <activity android:name=".MainNoiDung" />
        <activity android:name=".MainTimKiem" />
        <activity android:name=".MainDangKi" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MainDangNhap">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>